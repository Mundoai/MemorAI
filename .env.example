# MemorAI Configuration

# OpenRouter API Key (required - used for Mem0's LLM processing)
# Get free key at https://openrouter.ai
# Free models: stepfun/step-3.5-flash:free, arcee-ai/trinity-large-preview:free
OPENROUTER_API_KEY=

# Optional: Override the default free LLM model
# MEMORAI_LLM_MODEL=stepfun/step-3.5-flash:free

# Optional: API key to secure the memory API
MEMORAI_API_KEY=

# ----- PostgreSQL -----
POSTGRES_USER=memorai
POSTGRES_PASSWORD=memorai_secret
POSTGRES_DB=memorai

# ----- JWT -----
JWT_SECRET=change-me-to-a-random-secret

# ----- NextAuth (Dashboard) -----
# AUTH_URL: The canonical public URL of the dashboard. Required for production.
# Must match the actual URL users access (e.g. https://memoria.mywebsites.dev)
AUTH_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me-to-another-random-secret

# GitHub OAuth (https://github.com/settings/developers)
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Google OAuth (https://console.cloud.google.com/apis/credentials)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
